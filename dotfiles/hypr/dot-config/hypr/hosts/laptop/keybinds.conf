# QWERTY layout-specific keybindings
# Optimized for QWERTY keyboard layout (zaxtec laptop)

# Window management - QWERTY layout
bind = $mainMod SHIFT, Q, killactive,
bind = $mainMod, V, togglefloating,
bind = $mainMod SHIFT, V, pin

# File manager and menu - using QWERTY positions
bind = $mainMod, T, exec, $fileManager
bind = $mainMod, D, exec, $menu

# Window and layout management using natural QWERTY positions
bind = $mainMod, semicolon, togglesplit, # dwindle (avoiding J conflict with vim navigation)

# Movement with arrow keys (universal)
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Move windows with arrow keys
bind = $mainMod SHIFT, left, movewindow, l
bind = $mainMod SHIFT, right, movewindow, r
bind = $mainMod SHIFT, up, movewindow, u
bind = $mainMod SHIFT, down, movewindow, d
